<div class="container">
  <p>I wasn't sure how to split administration and viewing into seperate spaces</p>

  <h2>Here is a list of my various programming skills:</h2>


  <ul>
    <% @skills.each do |skill| %>
      <li><%= link_to skill.name, skill_path(skill) %>
        <%= link_to "Edit", edit_skill_path(skill), :class => 'glyphicon glyphicon-edit' %> |
        <%= link_to "Delete", skill_path(skill),
                                 :data => {:confirm => "Are you sure?",
                                                :method => "delete"}, :class => 'glyphicon glyphicon-remove' %>
      </li>
    <% end %>
  </ul>

  <%= link_to "Add New Skill", new_skill_path, :class=> 'btn btn-primary btn-sm' %>
</div>
